# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

# bundling : process of resolving the web of dependencies and merging(or 'packaging')
# the files(or modules) into optimized bundles for the browser with
# the goal of reducing the number of requests for files when a user
# visits a web page

# add jekyll as a dependency
# gem "jekyll" 

# gem "github-pages", group: :jekyll_plugins
